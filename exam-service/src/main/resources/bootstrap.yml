spring:
  devtools:
    restart:
      additional-paths: src/main/java
  application:
    name: exam-service
  cloud:
    config:
      uri: http://localhost:8081
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/exams?autorReconnect=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Tingeso2023#
  jpa:
    show-sql: true
    hibernate:
      dialect: MySQL8Dialect
      ddl-auto: create